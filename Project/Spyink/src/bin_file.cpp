#include <iostream>

#include "headers/bin_file.h"

void BinFile::hide(const std::string &input_filename, const std::string &password) const
{
    std::cout << "Unable to hide data in a binary file." << std::endl;
}

void BinFile::reveal(const std::string &input_filename, const std::string &password) const
{
    std::cout << "Unable to reveal data from a binary file." << std::endl;
}
